name: Daily Menu Scraper
on:
  schedule:
    # Runs at 14:00 UTC every day (6:00 AM PST)
    - cron: '0 14 * * *'
  workflow_dispatch: # Allows you to run it manually from the Actions tab

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        # This assumes you have a requirements.txt file in your scraper folder
        run: pip install -r scraper/requirements.txt

      - name: Run Scraper
        run: python scraper/scraper.py
        env:
          # These secrets must be added in GitHub Settings > Secrets and variables > Actions
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}